# Local Studio website

The standalone public website for Local Studio. The application and controller live in the separate `vllm-studio` repository.

## Development

```bash
npm install
npm run dev
```

The site runs on `http://localhost:3000` by default.
